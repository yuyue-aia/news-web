<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>新闻列表 - 新闻中心</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
        <div class="container">
            <a class="navbar-brand" href="/">新闻中心</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="/">首页</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/api/news">API</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <main class="container mt-4">
        <div class="row">
            <div class="col-12">
                <h1 class="mb-4">最新新闻</h1>
                
                <% if (news && news.length > 0) { %>
                    <div class="row">
                        <% news.forEach(function(article) { %>
                            <div class="col-md-6 col-lg-4 mb-4">
                                <div class="card h-100">
                                    <div class="card-body d-flex flex-column">
                                        <h5 class="card-title flex-grow-0">
                                            <a href="/news/<%= article._id %>" class="text-decoration-none">
                                                <%= article.title %>
                                            </a>
                                        </h5>
                                        <p class="card-text text-muted small flex-grow-0">
                                            <span class="badge bg-secondary me-2"><%= article.blog_type %></span>
                                            作者: <%= article.author %>
                                        </p>

                                        <% if (article.ai_summary) { %>
                                            <!-- AI 摘要（默认隐藏） -->
                                            <p class="card-text summary d-none flex-grow-1">
                                                <%= article.ai_summary %>
                                            </p>
                                            <button type="button" class="btn btn-sm btn-outline-primary toggle-summary align-self-start mt-auto">
                                                显示摘要
                                            </button>
                                        <% } else { %>
                                            <p class="card-text text-muted flex-grow-1">暂无摘要</p>
                                        <% } %>

                                        <div class="card-footer bg-transparent border-0 pt-3">
                                            <small class="text-muted">
                                                发布时间: <%= moment(article.publish_date).format('YYYY-MM-DD HH:mm') %>
                                            </small>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        <% }); %>
                    </div>
                    
                    <!-- 分页导航 -->
                    <% if (totalPages > 1) { %>
                        <nav aria-label="新闻分页">
                            <ul class="pagination justify-content-center">
                                <% if (currentPage > 1) { %>
                                    <li class="page-item">
                                        <a class="page-link" href="/?page=<%= currentPage - 1 %>">上一页</a>
                                    </li>
                                <% } %>
                                
                                <% for (let i = Math.max(1, currentPage - 2); i <= Math.min(totalPages, currentPage + 2); i++) { %>
                                    <li class="page-item <%= i === currentPage ? 'active' : '' %>">
                                        <a class="page-link" href="/?page=<%= i %>"><%= i %></a>
                                    </li>
                                <% } %>
                                
                                <% if (currentPage < totalPages) { %>
                                    <li class="page-item">
                                        <a class="page-link" href="/?page=<%= currentPage + 1 %>">下一页</a>
                                    </li>
                                <% } %>
                            </ul>
                        </nav>
                    <% } %>
                <% } else { %>
                    <div class="alert alert-info">
                        <h4>暂无新闻</h4>
                        <p>目前还没有新闻内容，请稍后再来查看。</p>
                    </div>
                <% } %>
            </div>
        </div>
    </main>

    <footer class="bg-light mt-5 py-4">
        <div class="container text-center">
            <p class="text-muted">&copy; 2025 新闻中心. All rights reserved.</p>
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
    <script src="/js/main.js"></script>
    <!-- 内联脚本：切换摘要显示 -->
    <script>
        document.addEventListener('DOMContentLoaded', function () {
            document.querySelectorAll('.toggle-summary').forEach(function (btn) {
                btn.addEventListener('click', function () {
                    const summary = btn.closest('.card-body').querySelector('.summary');
                    summary.classList.toggle('d-none');
                    btn.textContent = summary.classList.contains('d-none') ? '显示摘要' : '隐藏摘要';
                });
            });
        });
    </script>
</body>
</html>
