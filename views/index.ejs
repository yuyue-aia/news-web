<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>GeekNews - 极客新闻</title>
    <!-- Google Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&family=JetBrains+Mono:wght@400;500;600&display=swap" rel="stylesheet">
    <!-- Bootstrap -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Bootstrap Icons -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css" rel="stylesheet">
    <!-- Custom Styles -->
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <!-- ===== 顶部导航 ===== -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
        <div class="container">
            <a class="navbar-brand" href="/">GeekNews</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item"><a class="nav-link" href="/">首页</a></li>
                    <li class="nav-item"><a class="nav-link" href="/api/news">API</a></li>
                </ul>
            </div>
        </div>
    </nav>

    <!-- ===== 主内容区 ===== -->
    <main class="container my-4">
        <h1 class="mb-4">// 最新资讯</h1>

        <% if (news && news.length > 0) { %>
            <!-- Feed流容器 -->
            <div id="news-feed" class="row g-3">
                <% news.forEach(function(article, index) { %>
                    <div class="news-row" data-news-id="<%= article._id %>" data-index="<%= index %>">
                        <div class="news-content">
                            <div class="news-header">
                                <span class="news-index"><%= String(index + 1).padStart(2, '0') %></span>
                                <span class="news-category"><%= article.blog_type %></span>
                                <span class="news-time"><%= moment(article.publish_date).format('MM-DD HH:mm') %></span>
                            </div>
                            <h3 class="news-title"><%= article.title %></h3>
                            <div class="news-meta">
                                <span class="news-author"><%= article.author %></span>
                                <div class="news-actions">
                                    <% if (article.ai_summary) { %>
                                        <button class="action-btn summary-btn summary-icon" 
                                                data-summary="<%- article.ai_summary.replace(/\"/g, '&quot;') %>"
                                                title="AI摘要">
                                            <i class="bi bi-stars"></i>
                                        </button>
                                    <% } %>
                                    <% if (article.article_link) { %>
                                        <a href="<%= article.article_link %>" 
                                           target="_blank" 
                                           class="action-btn link-btn"
                                           title="原文">
                                            <i class="bi bi-box-arrow-up-right"></i>
                                        </a>
                                    <% } %>
                                </div>
                            </div>
                        </div>
                    </div>
                <% }); %>
            </div>

            <!-- 加载更多按钮 -->
            <% if (hasMore) { %>
                <div class="text-center mt-4">
                    <button id="load-more-btn" class="btn btn-outline-primary btn-lg">
                        <i class="bi bi-arrow-down-circle me-2"></i>加载更多
                    </button>
                </div>
            <% } %>
            
            <!-- 加载中指示器 -->
            <div id="loading-indicator" class="text-center mt-4" style="display: none;">
                <div class="spinner-border text-primary" role="status">
                    <span class="visually-hidden">加载中...</span>
                </div>
                <p class="mt-2 text-muted">正在加载更多新闻...</p>
            </div>
            
            <!-- 加载完成指示器 -->
            <div id="end-indicator" class="text-center mt-4" style="display: none;">
                <p class="text-muted">
                    <i class="bi bi-check-circle me-2"></i>已加载全部 <%= totalNews %> 条新闻
                </p>
            </div>
        <% } else { %>
            <div class="alert alert-info">
                <h4 class="alert-heading mb-2">暂无新闻</h4>
                <p class="mb-0">目前还没有新闻内容，请稍后再来查看。</p>
            </div>
        <% } %>
    </main>

    <!-- ===== 页脚 ===== -->
    <footer class="bg-light py-4 mt-5">
        <div class="container text-center">
            <p class="text-muted mb-0">GeekNews &copy; 2025 // Built for Developers & Tech Enthusiasts</p>
        </div>
    </footer>

    <!-- ===== 依赖脚本 ===== -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
    
    <!-- Feed流状态初始化 -->
    <script>
        // 初始化Feed流状态
        window.feedState = {
            currentOffset: <%= news.length %>,
            hasMore: <%= hasMore ? 'true' : 'false' %>,
            loading: false,
            limit: 10
        };
    </script>
    
    <script src="/js/main.js"></script>


</body>
</html>
